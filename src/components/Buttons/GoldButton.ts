import { styled } from '@mui/material/styles'

export default styled('button')(() => ({
  backgroundColor: 'gold',
  color: 'gray',
  border: 'none',
  padding: '10px',
  cursor: 'pointer',
  borderRadius: '5px',
}))
